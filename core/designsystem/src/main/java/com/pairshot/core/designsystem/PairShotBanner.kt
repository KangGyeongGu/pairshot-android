package com.pairshot.core.designsystem

import androidx.compose.ui.unit.dp

/** Banner ad dimensions. */
object PairShotBanner {
    /** Fallback height when adaptive banner size is unavailable (matches AdMob's
        recommended 320×50 banner at typical phone width). */
    val adaptiveFallbackHeight = 62.dp
}
