package com.pairshot.core.designsystem

import androidx.compose.ui.unit.dp

/** App bar / action bar heights. */
object PairShotAppBar {
    /** Top app bar standard height. */
    val height = 64.dp

    /** Bottom action bar standard height (Scaffold bottomBar slot). */
    val actionBarHeight = 72.dp
}
