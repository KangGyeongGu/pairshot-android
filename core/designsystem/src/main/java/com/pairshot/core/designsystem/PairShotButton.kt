package com.pairshot.core.designsystem

import androidx.compose.ui.unit.dp

/** Button height variants. Used by HomePrimaryActionBar and similar primary CTAs. */
object PairShotButton {
    /** Primary CTA bar action height. Slightly taller than touchTarget.standard for visual weight. */
    val primaryActionHeight = 52.dp

    /** FAB-style action button size (square). */
    val fabSize = 56.dp
}
