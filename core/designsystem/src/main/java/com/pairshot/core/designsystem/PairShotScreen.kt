package com.pairshot.core.designsystem

import androidx.compose.ui.unit.dp

/**
 * Screen-level layout dimensions.
 * For grid/marketing screens that diverge from the default, define screen-local
 * private vals instead — these tokens are for the default reading-content padding.
 */
object PairShotScreen {
    /** Horizontal padding for settings / text-content lists. */
    val horizontalPadding = 20.dp
}
